{
  "name": "wifi-traffic-analyzer",
  "version": "1.0.0",
  "description": "Complete Wi-Fi Traffic Analyzer with Security Features",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["wifi", "network", "analyzer", "security", "traffic"],
  "author": "WiFi Analyzer Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}